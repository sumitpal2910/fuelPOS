<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_grey_login_otp"
    android:paddingLeft="@dimen/bg_padding"
    android:paddingRight="@dimen/bg_padding"
    android:paddingBottom="@dimen/bg_padding">


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/imgv_super_scan_text_logo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginBottom="@dimen/super_scan_text_logo_margin_bottom"
            android:scaleType="fitXY"
            android:src="@drawable/goparkk" />

        <TextView
            android:id="@+id/txtv_tag_line"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textAlignment="center"
            android:textColor="@color/bg_text_dark_black"
            android:textSize="@dimen/textsize_30sp"
            android:textStyle="bold" />

    </LinearLayout>

    <!--<ImageView
        android:id="@+id/imgv_powered_by"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/splash_poweredby"
        android:scaleType="fitXY"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true" />-->

</RelativeLayout>